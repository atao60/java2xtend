// the two last instructions are on the same line in unformatted Xtend generated code
class BitwiseAnd {
    def void foo() {
        var int x=1.bitwiseAnd(2) 
        x=1.bitwiseOr(2) 
        x=3 % 4 
    }
    
}